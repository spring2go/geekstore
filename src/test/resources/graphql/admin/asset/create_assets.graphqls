mutation CreateAssets($files: [Upload!]!) {
    createAssets(files: $files) {
        id
        name
        fileSize
        mimeType
        type
        preview
        source
        focalPoint {
            x
            y
        }
    }
}